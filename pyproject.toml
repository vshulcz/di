[project]
name = "injex"
version = "0.1.0"
description = "DI container for Python applications"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "mypy>=1.12.1",
    "pytest>=8.3.3",
    "ruff>=0.7.0",
]
